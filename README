// Ahmed Ahmed
// 12/17/23
// CS 4760
// Project 2 (REDO)

To run the program, use the given Makefile to compile the `oss` and `worker` executables. Run the following command:

"make"

Then run the program using: "./oss -n -s -t" (putting in whatever numbers you want to run it with)


   
   



Comments:
My original project 2 was completely dysfunctional and all over the place, so I instead just scrapped it and strarted back from scratch. I just rewrote the whole thing, I didn't make any specific changes.
